services:
  lumora-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lumora-app
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
